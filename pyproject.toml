[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oauthlens"
version = "2.0.0"
description = "Learn how OAuth 2.0 works by running it, then add OAuth login to FastAPI in one command. Interactive OAuth debugger, 6 providers, PKCE."
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
keywords = [
    "oauth", "oauth2", "oauth2.0", "openid-connect", "oidc", "pkce",
    "fastapi", "authentication", "authorization", "login", "sso",
    "social-login", "sign-in-with-google", "sign-in-with-github",
    "oauth-debugger", "oauth-tutorial", "oauth-example", "learn-oauth",
    "github", "google", "discord", "spotify", "microsoft", "linkedin",
    "cli", "scaffold", "security",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Framework :: FastAPI",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Security",
]

[project.urls]
Homepage = "https://github.com/pranavkumaarofficial/oauthlens"
Repository = "https://github.com/pranavkumaarofficial/oauthlens"
Issues = "https://github.com/pranavkumaarofficial/oauthlens/issues"

[project.scripts]
oauthlens = "oauthlens.cli:main"
oauth-init = "oauthlens.cli:main"

[tool.setuptools.packages.find]
include = ["oauthlens*"]

[tool.setuptools.package-data]
oauthlens = ["scaffold/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
